Place PDFs here so Astro serves them from `/Documents/…` on the live site.

**Two resumes** (paths must match `src/site.config.ts` → `resumes`):

| File | Used for |
|------|-----------|
| `resume-software-engineering.pdf` | Software engineering resume tab |
| `resume-game-development.pdf` | Game development resume tab |

If your files use different names, either rename them to match or edit the `file` field for each entry in `src/site.config.ts`.
